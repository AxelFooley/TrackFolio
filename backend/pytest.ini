[pytest]
asyncio_mode = auto
asyncio_default_fixture_scope = function
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: integration tests that require external services (database, cache, etc.)
    unit: unit tests with mocked dependencies
    asyncio: mark test as async
